# Editorial / brand photography

Used by the About sections. Expected files:

| File | Used in |
|---|---|
| `warehouse.jpg` | Home → About preview (framed image) |
| `port.jpg` | about.html → story image |

Direction: warehouse/port operations at golden hour, container ships, bulk
sacks — warm, dignified, abundant. ~4:5 or 16:10, ≥1400px, warm grade.
Descriptive `alt` text is already set in the HTML.
